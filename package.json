{
  "name": "media-tracker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nuxt dev",
    "build": "npx prisma generate && npx prisma db push --accept-data-loss && npx nuxt build",
    "preview": "nuxt preview",
    "postinstall": "npx prisma generate",
    "test": "vitest run"
  },
  "dependencies": {
    "@prisma/client": "^5.15.0",
    "naive-ui": "^2.38.0",
    "nuxt": "^3.12.0",
    "prisma": "^5.15.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
